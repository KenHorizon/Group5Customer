function openButtons(target) {
	target.classList.add("show");
	console.log("button has clicked!");
}
function exitButtons(target) {
	target.classList.remove("show");
	console.log("button has clicked!");
}
